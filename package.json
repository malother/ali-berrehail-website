{
  "name": "ali-berrehail-website",
  "private": true,
  "scripts": {
    "build": "npm --prefix site run build",
    "postinstall": "npm --prefix site install"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
